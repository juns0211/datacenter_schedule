# 操作說明
clean.py 主要作為資料清洗使用, 定義func名稱後, 放在swagger api參數 "process_clean":["clean:func_name"]

params.py 主要作為排程邏輯使用, 定義func名稱後, 放在swagger api參數 "process_params":["params:func_name"]

storage.py 主要作為排程邏輯使用, 定義func名稱後, 放在swagger api參數 "process_storage":["storage:func_name"]